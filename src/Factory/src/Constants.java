package comp1549payrollproject;

/**
 * Constants used in the payroll calculations
 * 
 * In an operational system values like these would normally be read from a database or file
 * rather than hard-coded in the program itself.
 */
public interface Constants {
    public static final float VAT = 20.0F;
    public static final float UNSOCIAL_RATE_MULTIPLIER = 1.5F;
}
